\section{Appendix $A$: Software}

The following Python 3 code computes the binomial tail and finds the
smallest sufficient number of parity disks, including zero.
It is intended for small erasure-code groups.

{\small
\verbatiminput{sample_code.py}
}

